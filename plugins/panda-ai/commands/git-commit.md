---
description: Generate a conventional commit message from current git changes and ask what to do (show only / commit / commit + push). Usage: /panda-ai:git-commit
---

Use the `panda-ai:git-commit` skill.
